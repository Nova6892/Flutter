import 'package:flutter/material.dart';

void main(){
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: const Text('Inserting an Image'),
          centerTitle: true,
        ),
        body: Container(
          child: Column(
            children: <Widget>[
              Image.asset('Path//Also update in pubspec.yaml')
            ],
          ),
        ),
      ),
    );
  }
}
